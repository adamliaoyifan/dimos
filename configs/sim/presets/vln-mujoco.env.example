# Copy to a private path or merge into .env — VLN + Go2 MuJoCo.
# Point VLN_CONFIG at a customized copy of configs/sim/vln.example.yaml (or your repo's vln_config.yaml).

DIMOS_SIMULATION=true
# DIMOS_MUJOCO_ROOM=office1
# DIMOS_MUJOCO_START_POS=-1.0, 1.0
DIMOS_VIEWER=rerun

# If your stack loads VLN from YAML:
# export VLN_CONFIG=/absolute/path/to/my_vln.yaml
